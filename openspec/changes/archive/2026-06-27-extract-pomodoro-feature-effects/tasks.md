# Tasks

- [x] 1. 增加 `PomodoroFeature` direct smoke test，先证明当前缺少 feature owner。
- [x] 2. 实现 `PomodoroFeature` 和 effects model。
- [x] 3. 让 `TaskCoordinator` 委托 Pomodoro start/cancel/advance，并保持现有公开状态。
- [x] 4. 更新机会池和 OpenSpec spec。
- [x] 5. 运行 `scripts/test.sh`、`scripts/build.sh`、`openspec validate --all --strict`、`git diff --check`，归档完成的 change。

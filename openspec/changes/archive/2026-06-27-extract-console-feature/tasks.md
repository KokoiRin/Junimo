# Tasks

- [x] 1. 增加 `ConsoleFeature` direct smoke test，先证明当前缺少 console feature owner。
- [x] 2. 实现 `ConsoleFeature` 和 action effects model。
- [x] 3. 让 `TaskCoordinator` 委托 command query、action execution、activity/session refresh 给 `ConsoleFeature`。
- [x] 4. 更新机会池和 OpenSpec spec。
- [x] 5. 运行 `scripts/test.sh`、`scripts/build.sh`、`openspec validate --all --strict`、`git diff --check`，归档完成的 change。

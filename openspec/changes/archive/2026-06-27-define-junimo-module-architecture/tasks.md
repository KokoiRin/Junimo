# Tasks

- [x] 1. Document the target module structure, dependency direction, component contracts, and migration order.
- [x] 2. Add `module-architecture` requirements covering state ownership, shell/runtime boundaries, adapters, diagnostics, and compatibility coordinator behavior.
- [x] 3. Define the first Codex walking skeleton task package: feature state, intents/events/effects, reducer tests, adapter split, coordinator delegation, and diagnostics snapshot.
- [x] 4. Validate the OpenSpec change with `openspec validate define-junimo-module-architecture --strict`.
- [x] 5. After user approval, implement the first Codex feature-store slice with reducer-first tests.

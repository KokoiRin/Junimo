# Tasks

- [x] 1. 增加 `PreferencesFeature` direct smoke test，先证明当前缺少 preferences feature owner。
- [x] 2. 实现 `PreferencesFeature`。
- [x] 3. 让 `TaskCoordinator` 委托 preferences/theme 投影和 accent/density intents 给 feature。
- [x] 4. 更新机会池和 OpenSpec spec。
- [x] 5. 运行 `scripts/test.sh`、`scripts/build.sh`、`openspec validate --all --strict`、`git diff --check`，归档完成的 change。

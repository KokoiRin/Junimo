# Tasks

- [x] 1. Add a direct test for `CornerNoteFeature` expansion and note/todo mutations.
- [x] 2. Implement `CornerNoteFeature` over `CornerNoteCore`.
- [x] 3. Delegate `TaskCoordinator` Corner Note state and intents to `CornerNoteFeature`.
- [x] 4. Update docs and `chowa/opportunities.md`.
- [x] 5. Run `scripts/test.sh`, `scripts/build.sh`, `openspec validate --all --strict`, `git diff --check`, and `scripts/verify_ci.sh`.
- [x] 6. Archive the OpenSpec change.
